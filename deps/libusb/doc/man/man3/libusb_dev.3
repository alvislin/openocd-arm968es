.TH "libusb_dev" 3 "Version 1.0.30" "libusb" \" -*- nroff -*-
.ad l
.nh
.SH NAME
libusb_dev \- Device handling and enumeration
.PP
 \- The functionality documented below is designed to help with the following operations:  

.SH SYNOPSIS
.br
.PP
.SS "Typedefs"

.in +1c
.ti -1c
.RI "typedef struct \fBlibusb_device\fP \fBlibusb_device\fP"
.br
.RI "Structure representing a USB device detected on the system\&. "
.ti -1c
.RI "typedef struct \fBlibusb_device_handle\fP \fBlibusb_device_handle\fP"
.br
.RI "Structure representing a handle on a USB device\&. "
.in -1c
.SS "Enumerations"

.in +1c
.ti -1c
.RI "enum \fBlibusb_speed\fP { \fBLIBUSB_SPEED_UNKNOWN\fP = 0, \fBLIBUSB_SPEED_LOW\fP = 1, \fBLIBUSB_SPEED_FULL\fP = 2, \fBLIBUSB_SPEED_HIGH\fP = 3, \fBLIBUSB_SPEED_SUPER\fP = 4, \fBLIBUSB_SPEED_SUPER_PLUS\fP = 5, \fBLIBUSB_SPEED_SUPER_PLUS_X2\fP = 6 }"
.br
.RI "Speed codes\&. "
.in -1c
.SS "Functions"

.in +1c
.ti -1c
.RI "ssize_t \fBlibusb_get_device_list\fP (\fBlibusb_context\fP *ctx, \fBlibusb_device\fP ***list)"
.br
.RI "Returns a list of USB devices currently attached to the system\&. "
.ti -1c
.RI "void \fBlibusb_free_device_list\fP (\fBlibusb_device\fP **list, int unref_devices)"
.br
.RI "Frees a list of devices previously discovered using \fBlibusb_get_device_list()\fP\&. "
.ti -1c
.RI "unsigned long \fBlibusb_get_session_data\fP (\fBlibusb_device\fP *dev)"
.br
.RI "Returns the backend-specific identifier of the underlying system device tree node\&. "
.ti -1c
.RI "uint8_t \fBlibusb_get_bus_number\fP (\fBlibusb_device\fP *dev)"
.br
.RI "Get the number of the bus that a device is connected to\&. "
.ti -1c
.RI "uint8_t \fBlibusb_get_port_number\fP (\fBlibusb_device\fP *dev)"
.br
.RI "Get the number of the port that a device is connected to\&. "
.ti -1c
.RI "int \fBlibusb_get_port_numbers\fP (\fBlibusb_device\fP *dev, uint8_t *port_numbers, int port_numbers_len)"
.br
.RI "Get the list of all port numbers from root for the specified device\&. "
.ti -1c
.RI "int \fBlibusb_get_port_path\fP (\fBlibusb_context\fP *ctx, \fBlibusb_device\fP *dev, uint8_t *port_numbers, uint8_t port_numbers_len)"
.br
.ti -1c
.RI "\fBlibusb_device\fP * \fBlibusb_get_parent\fP (\fBlibusb_device\fP *dev)"
.br
.RI "Get the the parent from the specified device\&. "
.ti -1c
.RI "uint8_t \fBlibusb_get_device_address\fP (\fBlibusb_device\fP *dev)"
.br
.RI "Get the address of the device on the bus it is connected to\&. "
.ti -1c
.RI "int \fBlibusb_get_device_speed\fP (\fBlibusb_device\fP *dev)"
.br
.RI "Get the negotiated connection speed for a device\&. "
.ti -1c
.RI "int \fBlibusb_get_max_packet_size\fP (\fBlibusb_device\fP *dev, unsigned char endpoint)"
.br
.RI "Convenience function to retrieve the wMaxPacketSize value for a particular endpoint in the active device configuration\&. "
.ti -1c
.RI "int \fBlibusb_get_max_iso_packet_size\fP (\fBlibusb_device\fP *dev, unsigned char endpoint)"
.br
.RI "Calculate the maximum packet size which a specific endpoint is capable is sending or receiving in the duration of 1 microframe\&. "
.ti -1c
.RI "int \fBlibusb_get_max_alt_packet_size\fP (\fBlibusb_device\fP *dev, int interface_number, int alternate_setting, unsigned char endpoint)"
.br
.RI "Calculate the maximum packet size which a specific endpoint is capable of sending or receiving in the duration of 1 microframe\&. "
.ti -1c
.RI "\fBlibusb_device\fP * \fBlibusb_ref_device\fP (\fBlibusb_device\fP *dev)"
.br
.RI "Increment the reference count of a device\&. "
.ti -1c
.RI "void \fBlibusb_unref_device\fP (\fBlibusb_device\fP *dev)"
.br
.RI "Decrement the reference count of a device\&. "
.ti -1c
.RI "int \fBlibusb_wrap_sys_device\fP (\fBlibusb_context\fP *ctx, intptr_t sys_dev, \fBlibusb_device_handle\fP **dev_handle)"
.br
.RI "Wrap a platform-specific system device handle and obtain a libusb device handle for the underlying device\&. "
.ti -1c
.RI "int \fBlibusb_open\fP (\fBlibusb_device\fP *dev, \fBlibusb_device_handle\fP **dev_handle)"
.br
.RI "Open a device and obtain a device handle\&. "
.ti -1c
.RI "\fBlibusb_device_handle\fP * \fBlibusb_open_device_with_vid_pid\fP (\fBlibusb_context\fP *ctx, uint16_t vendor_id, uint16_t product_id)"
.br
.RI "Convenience function for finding a device with a particular \fCidVendor\fP/\fCidProduct\fP combination\&. "
.ti -1c
.RI "void \fBlibusb_close\fP (\fBlibusb_device_handle\fP *dev_handle)"
.br
.RI "Close a device handle\&. "
.ti -1c
.RI "\fBlibusb_device\fP * \fBlibusb_get_device\fP (\fBlibusb_device_handle\fP *dev_handle)"
.br
.RI "Get the underlying device for a device handle\&. "
.ti -1c
.RI "int \fBlibusb_get_configuration\fP (\fBlibusb_device_handle\fP *dev_handle, int *config)"
.br
.RI "Determine the bConfigurationValue of the currently active configuration\&. "
.ti -1c
.RI "int \fBlibusb_set_configuration\fP (\fBlibusb_device_handle\fP *dev_handle, int configuration)"
.br
.RI "Set the active configuration for a device\&. "
.ti -1c
.RI "int \fBlibusb_claim_interface\fP (\fBlibusb_device_handle\fP *dev_handle, int interface_number)"
.br
.RI "Claim an interface on a given device handle\&. "
.ti -1c
.RI "int \fBlibusb_release_interface\fP (\fBlibusb_device_handle\fP *dev_handle, int interface_number)"
.br
.RI "Release an interface previously claimed with \fBlibusb_claim_interface()\fP\&. "
.ti -1c
.RI "int \fBlibusb_set_interface_alt_setting\fP (\fBlibusb_device_handle\fP *dev_handle, int interface_number, int alternate_setting)"
.br
.RI "Activate an alternate setting for an interface\&. "
.ti -1c
.RI "int \fBlibusb_clear_halt\fP (\fBlibusb_device_handle\fP *dev_handle, unsigned char endpoint)"
.br
.RI "Clear the halt/stall condition for an endpoint\&. "
.ti -1c
.RI "int \fBlibusb_reset_device\fP (\fBlibusb_device_handle\fP *dev_handle)"
.br
.RI "Perform a USB port reset to reinitialize a device\&. "
.ti -1c
.RI "int \fBlibusb_kernel_driver_active\fP (\fBlibusb_device_handle\fP *dev_handle, int interface_number)"
.br
.RI "Determine if a kernel driver is active on an interface\&. "
.ti -1c
.RI "int \fBlibusb_detach_kernel_driver\fP (\fBlibusb_device_handle\fP *dev_handle, int interface_number)"
.br
.RI "Detach a kernel driver from an interface\&. "
.ti -1c
.RI "int \fBlibusb_attach_kernel_driver\fP (\fBlibusb_device_handle\fP *dev_handle, int interface_number)"
.br
.RI "Re-attach an interface's kernel driver, which was previously detached using \fBlibusb_detach_kernel_driver()\fP\&. "
.ti -1c
.RI "int \fBlibusb_set_auto_detach_kernel_driver\fP (\fBlibusb_device_handle\fP *dev_handle, int enable)"
.br
.RI "Enable/disable libusb's automatic kernel driver detachment\&. "
.ti -1c
.RI "int \fBlibusb_endpoint_supports_raw_io\fP (\fBlibusb_device_handle\fP *dev_handle, uint8_t endpoint)"
.br
.RI "Check if the endpoint supports RAW_IO\&. "
.ti -1c
.RI "int \fBlibusb_endpoint_set_raw_io\fP (\fBlibusb_device_handle\fP *dev_handle, uint8_t endpoint, int enable)"
.br
.RI "Enable/disable RAW_IO for an endpoint on an open device\&. "
.ti -1c
.RI "int \fBlibusb_get_max_raw_io_transfer_size\fP (\fBlibusb_device_handle\fP *dev_handle, uint8_t endpoint)"
.br
.RI "Retrieve the maximum transfer size in bytes supported for WinUSB RAW_IO for an inbound bulk or interrupt endpoint on an open device\&. "
.in -1c
.SH "Detailed Description"
.PP 
The functionality documented below is designed to help with the following operations: 


.IP "\(bu" 2
Enumerating the USB devices currently attached to the system
.IP "\(bu" 2
Choosing a device to operate from your software
.IP "\(bu" 2
Opening and closing the chosen device
.PP
.SH "In a nutshell\&.\&.\&."
.PP
The description below really makes things sound more complicated than they actually are\&. The following sequence of function calls will be suitable for almost all scenarios and does not require you to have such a deep understanding of the resource management issues: 
.PP
.nf
// discover devices
libusb_device **list;
libusb_device *found = NULL;
ssize_t cnt = libusb_get_device_list(NULL, &list);
ssize_t i = 0;
int err = 0;
if (cnt < 0)
    error();

for (i = 0; i < cnt; i++) {
    libusb_device *device = list[i];
    if (is_interesting(device)) {
        found = device;
        break;
    }
}

if (found) {
    libusb_device_handle *handle;

    err = libusb_open(found, &handle);
    if (err)
        error();
    // etc
}

libusb_free_device_list(list, 1);

.fi
.PP
.PP
The two important points:
.IP "\(bu" 2
You asked \fBlibusb_free_device_list()\fP to unreference the devices (2nd parameter)
.IP "\(bu" 2
You opened the device before freeing the list and unreferencing the devices
.PP
.PP
If you ended up with a handle, you can now proceed to perform I/O on the device\&.
.SH "Devices and device handles"
.PP
libusb has a concept of a USB device, represented by the \fBlibusb_device\fP opaque type\&. A device represents a USB device that is currently or was previously connected to the system\&. Using a reference to a device, you can determine certain information about the device (e\&.g\&. you can read the descriptor data)\&.
.PP
The \fBlibusb_get_device_list()\fP function can be used to obtain a list of devices currently connected to the system\&. This is known as device discovery\&. Devices can also be discovered with the hotplug mechanism, whereby a callback function registered with \fBlibusb_hotplug_register_callback()\fP will be called when a device of interest is connected or disconnected\&.
.PP
Just because you have a reference to a device does not mean it is necessarily usable\&. The device may have been unplugged, you may not have permission to operate such device, or another program or driver may be using the device\&.
.PP
When you've found a device that you'd like to operate, you must ask libusb to open the device using the \fBlibusb_open()\fP function\&. Assuming success, libusb then returns you a \fIdevice handle\fP (a \fBlibusb_device_handle\fP pointer)\&. All 'real' I/O operations then operate on the handle rather than the original device pointer\&.
.SH "Device discovery and reference counting"
.PP
Device discovery (i\&.e\&. calling \fBlibusb_get_device_list()\fP) returns a freshly-allocated list of devices\&. The list itself must be freed when you are done with it\&. libusb also needs to know when it is OK to free the contents of the list - the devices themselves\&.
.PP
To handle these issues, libusb provides you with two separate items:
.IP "\(bu" 2
A function to free the list itself
.IP "\(bu" 2
A reference counting system for the devices inside
.PP
.PP
New devices presented by the \fBlibusb_get_device_list()\fP function all have a reference count of 1\&. You can increase and decrease reference count using \fBlibusb_ref_device()\fP and \fBlibusb_unref_device()\fP\&. A device is destroyed when its reference count reaches 0\&.
.PP
With the above information in mind, the process of opening a device can be viewed as follows:
.IP "1." 4
Discover devices using \fBlibusb_get_device_list()\fP or \fBlibusb_hotplug_register_callback()\fP\&.
.IP "2." 4
Choose the device that you want to operate, and call \fBlibusb_open()\fP\&.
.IP "3." 4
Unref all devices in the discovered device list\&.
.IP "4." 4
Free the discovered device list\&.
.PP
.PP
The order is important - you must not unreference the device before attempting to open it, because unreferencing it may destroy the device\&.
.PP
For convenience, the \fBlibusb_free_device_list()\fP function includes a parameter to optionally unreference all the devices in the list before freeing the list itself\&. This combines steps 3 and 4 above\&.
.PP
As an implementation detail, \fBlibusb_open()\fP actually adds a reference to the device in question\&. This is because the device remains available through the handle via \fBlibusb_get_device()\fP\&. The reference is deleted during \fBlibusb_close()\fP\&. 
.SH "Typedef Documentation"
.PP 
.SS "typedef struct \fBlibusb_device\fP \fBlibusb_device\fP"

.PP
Structure representing a USB device detected on the system\&. This is an opaque type for which you are only ever provided with a pointer, usually originating from \fBlibusb_get_device_list()\fP or \fBlibusb_hotplug_register_callback()\fP\&.
.PP
Certain operations can be performed on a device, but in order to do any I/O you will have to first obtain a device handle using \fBlibusb_open()\fP\&.
.PP
Devices are reference counted with \fBlibusb_ref_device()\fP and \fBlibusb_unref_device()\fP, and are freed when the reference count reaches 0\&. New devices presented by \fBlibusb_get_device_list()\fP have a reference count of 1, and \fBlibusb_free_device_list()\fP can optionally decrease the reference count on all devices in the list\&. \fBlibusb_open()\fP adds another reference which is later destroyed by \fBlibusb_close()\fP\&. 
.SS "typedef struct \fBlibusb_device_handle\fP \fBlibusb_device_handle\fP"

.PP
Structure representing a handle on a USB device\&. This is an opaque type for which you are only ever provided with a pointer, usually originating from \fBlibusb_open()\fP\&.
.PP
A device handle is used to perform I/O and other operations\&. When finished with a device handle, you should call \fBlibusb_close()\fP\&. 
.SH "Enumeration Type Documentation"
.PP 
.SS "enum \fBlibusb_speed\fP"

.PP
Speed codes\&. Indicates the speed at which the device is operating\&. 
.PP
\fBEnumerator\fP
.in +1c
.TP
\fB\fILIBUSB_SPEED_UNKNOWN \fP\fP
The OS doesn't report or know the device speed\&. 
.TP
\fB\fILIBUSB_SPEED_LOW \fP\fP
The device is operating at low speed (1\&.5MBit/s)\&. 
.TP
\fB\fILIBUSB_SPEED_FULL \fP\fP
The device is operating at full speed (12MBit/s)\&. 
.TP
\fB\fILIBUSB_SPEED_HIGH \fP\fP
The device is operating at high speed (480MBit/s)\&. 
.TP
\fB\fILIBUSB_SPEED_SUPER \fP\fP
The device is operating at super speed (5000MBit/s)\&. 
.TP
\fB\fILIBUSB_SPEED_SUPER_PLUS \fP\fP
The device is operating at super speed plus (10000MBit/s)\&. 
.TP
\fB\fILIBUSB_SPEED_SUPER_PLUS_X2 \fP\fP
The device is operating at super speed plus x2 (20000MBit/s)\&. 
.SH "Function Documentation"
.PP 
.SS "ssize_t libusb_get_device_list (\fBlibusb_context\fP * ctx, \fBlibusb_device\fP *** list)"

.PP
Returns a list of USB devices currently attached to the system\&. This is your entry point into finding a USB device to operate\&.
.PP
You are expected to unreference all the devices when you are done with them, and then free the list with \fBlibusb_free_device_list()\fP\&. Note that \fBlibusb_free_device_list()\fP can unref all the devices for you\&. Be careful not to unreference a device you are about to open until after you have opened it\&.
.PP
This return value of this function indicates the number of devices in the resultant list\&. The list is actually one element larger, as it is NULL-terminated\&.
.PP
\fBParameters\fP
.RS 4
\fIctx\fP the context to operate on, or NULL for the default context 
.br
\fIlist\fP output location for a list of devices\&. Must be later freed with \fBlibusb_free_device_list()\fP\&. 
.RE
.PP
\fBReturns\fP
.RS 4
the number of devices in the outputted list, or any \fBlibusb_error\fP according to errors encountered by the backend\&. 
.RE
.PP

.SS "void libusb_free_device_list (\fBlibusb_device\fP ** list, int unref_devices)"

.PP
Frees a list of devices previously discovered using \fBlibusb_get_device_list()\fP\&. If the unref_devices parameter is set, the reference count of each device in the list is decremented by 1\&. 
.PP
\fBParameters\fP
.RS 4
\fIlist\fP the list to free 
.br
\fIunref_devices\fP whether to unref the devices in the list 
.RE
.PP

.SS "unsigned long libusb_get_session_data (\fBlibusb_device\fP * dev)"

.PP
Returns the backend-specific identifier of the underlying system device tree node\&. Can be used to find the corresponding system device and directly query it (or access it otherwise) when and if necessary\&.
.PP
Relevant backends:
.IP "\(bu" 2
Darwin: IOKit \fCsessionID\fP
.IP "\(bu" 2
Windows WinUSB: \fCDEVINST\fP
.IP "\(bu" 2
Linux, BSD: \fCbusnum << 8 | devnum\fP
.PP
.PP
Since version 1\&.0\&.30, \fBLIBUSB_API_VERSION\fP >= 0x0100010C
.PP
\fBParameters\fP
.RS 4
\fIdev\fP a device (must not be null) 
.RE
.PP
\fBReturns\fP
.RS 4
the backend-specific device identifier 
.RE
.PP

.SS "uint8_t libusb_get_bus_number (\fBlibusb_device\fP * dev)"

.PP
Get the number of the bus that a device is connected to\&. 
.PP
\fBParameters\fP
.RS 4
\fIdev\fP a device 
.RE
.PP
\fBReturns\fP
.RS 4
the bus number 
.RE
.PP

.SS "uint8_t libusb_get_port_number (\fBlibusb_device\fP * dev)"

.PP
Get the number of the port that a device is connected to\&. Unless the OS does something funky, or you are hot-plugging USB extension cards, the port number returned by this call is usually guaranteed to be uniquely tied to a physical port, meaning that different devices plugged on the same physical port should return the same port number\&.
.PP
But outside of this, there is no guarantee that the port number returned by this call will remain the same, or even match the order in which ports have been numbered by the HUB/HCD manufacturer\&.
.PP
\fBParameters\fP
.RS 4
\fIdev\fP a device 
.RE
.PP
\fBReturns\fP
.RS 4
the port number (0 if not available) 
.RE
.PP

.SS "int libusb_get_port_numbers (\fBlibusb_device\fP * dev, uint8_t * port_numbers, int port_numbers_len)"

.PP
Get the list of all port numbers from root for the specified device\&. Since version 1\&.0\&.16, \fBLIBUSBX_API_VERSION\fP >= 0x01000102 
.PP
\fBParameters\fP
.RS 4
\fIdev\fP a device 
.br
\fIport_numbers\fP the array that should contain the port numbers 
.br
\fIport_numbers_len\fP the maximum length of the array\&. As per the USB 3\&.0 specs, the current maximum limit for the depth is 7\&. 
.RE
.PP
\fBReturns\fP
.RS 4
the number of elements filled 
.PP
\fBLIBUSB_ERROR_OVERFLOW\fP if the array is too small 
.RE
.PP

.SS "int libusb_get_port_path (\fBlibusb_context\fP * ctx, \fBlibusb_device\fP * dev, uint8_t * port_numbers, uint8_t port_numbers_len)"

.PP
\fBDeprecated\fP
.RS 4
Please use \fBlibusb_get_port_numbers()\fP instead\&. 
.RE
.PP

.SS "\fBlibusb_device\fP * libusb_get_parent (\fBlibusb_device\fP * dev)"

.PP
Get the the parent from the specified device\&. 
.PP
\fBParameters\fP
.RS 4
\fIdev\fP a device 
.RE
.PP
\fBReturns\fP
.RS 4
the device parent or NULL if not available You should issue a \fBlibusb_get_device_list()\fP before calling this function and make sure that you only access the parent before issuing \fBlibusb_free_device_list()\fP\&. The reason is that libusb currently does not maintain a permanent list of device instances, and therefore can only guarantee that parents are fully instantiated within a \fBlibusb_get_device_list()\fP - \fBlibusb_free_device_list()\fP block\&. 
.RE
.PP

.SS "uint8_t libusb_get_device_address (\fBlibusb_device\fP * dev)"

.PP
Get the address of the device on the bus it is connected to\&. 
.PP
\fBParameters\fP
.RS 4
\fIdev\fP a device 
.RE
.PP
\fBReturns\fP
.RS 4
the device address 
.RE
.PP

.SS "int libusb_get_device_speed (\fBlibusb_device\fP * dev)"

.PP
Get the negotiated connection speed for a device\&. 
.PP
\fBParameters\fP
.RS 4
\fIdev\fP a device 
.RE
.PP
\fBReturns\fP
.RS 4
a \fBlibusb_speed\fP code, where LIBUSB_SPEED_UNKNOWN means that the OS doesn't know or doesn't support returning the negotiated speed\&. 
.RE
.PP

.SS "int libusb_get_max_packet_size (\fBlibusb_device\fP * dev, unsigned char endpoint)"

.PP
Convenience function to retrieve the wMaxPacketSize value for a particular endpoint in the active device configuration\&. This function was originally intended to be of assistance when setting up isochronous transfers, but a design mistake resulted in this function instead\&. It simply returns the wMaxPacketSize value without considering its contents\&. If you're dealing with isochronous transfers, you probably want \fBlibusb_get_max_iso_packet_size()\fP instead\&.
.PP
\fBParameters\fP
.RS 4
\fIdev\fP a device 
.br
\fIendpoint\fP address of the endpoint in question 
.RE
.PP
\fBReturns\fP
.RS 4
the wMaxPacketSize value 
.PP
\fBLIBUSB_ERROR_NOT_FOUND\fP if the endpoint does not exist 
.PP
\fBLIBUSB_ERROR_OTHER\fP on other failure 
.RE
.PP

.SS "int libusb_get_max_iso_packet_size (\fBlibusb_device\fP * dev, unsigned char endpoint)"

.PP
Calculate the maximum packet size which a specific endpoint is capable is sending or receiving in the duration of 1 microframe\&. Only the active configuration is examined\&. The calculation is based on the wMaxPacketSize field in the endpoint descriptor as described in section 9\&.6\&.6 in the USB 2\&.0 specifications\&.
.PP
If acting on an isochronous or interrupt endpoint, this function will multiply the value found in bits 0:10 by the number of transactions per microframe (determined by bits 11:12)\&. Otherwise, this function just returns the numeric value found in bits 0:10\&. For USB 3\&.0 device, it will attempts to retrieve the Endpoint Companion Descriptor to return wBytesPerInterval\&.
.PP
This function is useful for setting up isochronous transfers, for example you might pass the return value from this function to \fBlibusb_set_iso_packet_lengths()\fP in order to set the length field of every isochronous packet in a transfer\&.
.PP
This function only considers the first alternate setting of the interface\&. If the endpoint has different maximum packet sizes for different alternate settings, you probably want \fBlibusb_get_max_alt_packet_size()\fP instead\&.
.PP
Since v1\&.0\&.3\&.
.PP
\fBParameters\fP
.RS 4
\fIdev\fP a device 
.br
\fIendpoint\fP address of the endpoint in question 
.RE
.PP
\fBReturns\fP
.RS 4
the maximum packet size which can be sent/received on this endpoint 
.PP
\fBLIBUSB_ERROR_NOT_FOUND\fP if the endpoint does not exist 
.PP
\fBLIBUSB_ERROR_OTHER\fP on other failure 
.RE
.PP
\fBSee also\fP
.RS 4
\fBlibusb_get_max_alt_packet_size\fP 
.RE
.PP

.SS "int libusb_get_max_alt_packet_size (\fBlibusb_device\fP * dev, int interface_number, int alternate_setting, unsigned char endpoint)"

.PP
Calculate the maximum packet size which a specific endpoint is capable of sending or receiving in the duration of 1 microframe\&. Only the active configuration is examined\&. The calculation is based on the wMaxPacketSize field in the endpoint descriptor as described in section 9\&.6\&.6 in the USB 2\&.0 specifications\&.
.PP
If acting on an isochronous or interrupt endpoint, this function will multiply the value found in bits 0:10 by the number of transactions per microframe (determined by bits 11:12)\&. Otherwise, this function just returns the numeric value found in bits 0:10\&. For USB 3\&.0 device, it will attempts to retrieve the Endpoint Companion Descriptor to return wBytesPerInterval\&.
.PP
This function is useful for setting up isochronous transfers, for example you might pass the return value from this function to \fBlibusb_set_iso_packet_lengths()\fP in order to set the length field of every isochronous packet in a transfer\&.
.PP
Since version 1\&.0\&.27, \fBLIBUSB_API_VERSION\fP >= 0x0100010A
.PP
\fBParameters\fP
.RS 4
\fIdev\fP a device 
.br
\fIinterface_number\fP the \fCbInterfaceNumber\fP of the interface the endpoint belongs to 
.br
\fIalternate_setting\fP the \fCbAlternateSetting\fP of the interface 
.br
\fIendpoint\fP address of the endpoint in question 
.RE
.PP
\fBReturns\fP
.RS 4
the maximum packet size which can be sent/received on this endpoint 
.PP
\fBLIBUSB_ERROR_NOT_FOUND\fP if the endpoint does not exist 
.PP
\fBLIBUSB_ERROR_OTHER\fP on other failure 
.RE
.PP
\fBSee also\fP
.RS 4
\fBlibusb_get_max_iso_packet_size\fP 
.RE
.PP

.SS "\fBlibusb_device\fP * libusb_ref_device (\fBlibusb_device\fP * dev)"

.PP
Increment the reference count of a device\&. 
.PP
\fBParameters\fP
.RS 4
\fIdev\fP the device to reference 
.RE
.PP
\fBReturns\fP
.RS 4
the same device 
.RE
.PP

.SS "void libusb_unref_device (\fBlibusb_device\fP * dev)"

.PP
Decrement the reference count of a device\&. If the decrement operation causes the reference count to reach zero, the device shall be destroyed\&. 
.PP
\fBParameters\fP
.RS 4
\fIdev\fP the device to unreference 
.RE
.PP

.SS "int libusb_wrap_sys_device (\fBlibusb_context\fP * ctx, intptr_t sys_dev, \fBlibusb_device_handle\fP ** dev_handle)"

.PP
Wrap a platform-specific system device handle and obtain a libusb device handle for the underlying device\&. The handle allows you to use libusb to perform I/O on the device in question\&.
.PP
Call libusb_init_context with the LIBUSB_OPTION_NO_DEVICE_DISCOVERY option if you want to skip enumeration of USB devices\&. In particular, this might be needed on Android if you don't have authority to access USB devices in general\&. Setting this option with libusb_set_option is deprecated\&.
.PP
On Linux, the system device handle must be a valid file descriptor opened on the device node\&.
.PP
The system device handle must remain open until \fBlibusb_close()\fP is called\&. The system device handle will not be closed by \fBlibusb_close()\fP\&.
.PP
Internally, this function creates a temporary device and makes it available to you through \fBlibusb_get_device()\fP\&. This device is destroyed during \fBlibusb_close()\fP\&. The device shall not be opened through \fBlibusb_open()\fP\&.
.PP
This is a non-blocking function; no requests are sent over the bus\&.
.PP
Since version 1\&.0\&.23, \fBLIBUSB_API_VERSION\fP >= 0x01000107
.PP
\fBParameters\fP
.RS 4
\fIctx\fP the context to operate on, or NULL for the default context 
.br
\fIsys_dev\fP the platform-specific system device handle 
.br
\fIdev_handle\fP output location for the returned device handle pointer\&. Only populated when the return code is 0\&. 
.RE
.PP
\fBReturns\fP
.RS 4
0 on success 
.PP
\fBLIBUSB_ERROR_NO_MEM\fP on memory allocation failure 
.PP
\fBLIBUSB_ERROR_ACCESS\fP if the user has insufficient permissions 
.PP
\fBLIBUSB_ERROR_NOT_SUPPORTED\fP if the operation is not supported on this platform 
.PP
another LIBUSB_ERROR code on other failure 
.RE
.PP

.SS "int libusb_open (\fBlibusb_device\fP * dev, \fBlibusb_device_handle\fP ** dev_handle)"

.PP
Open a device and obtain a device handle\&. A handle allows you to perform I/O on the device in question\&.
.PP
Internally, this function adds a reference to the device and makes it available to you through \fBlibusb_get_device()\fP\&. This reference is removed during \fBlibusb_close()\fP\&.
.PP
This is a non-blocking function; no requests are sent over the bus\&.
.PP
\fBParameters\fP
.RS 4
\fIdev\fP the device to open 
.br
\fIdev_handle\fP output location for the returned device handle pointer\&. Only populated when the return code is 0\&. 
.RE
.PP
\fBReturns\fP
.RS 4
0 on success 
.PP
\fBLIBUSB_ERROR_NO_MEM\fP on memory allocation failure 
.PP
\fBLIBUSB_ERROR_ACCESS\fP if the user has insufficient permissions 
.PP
\fBLIBUSB_ERROR_NO_DEVICE\fP if the device has been disconnected 
.PP
another LIBUSB_ERROR code on other failure 
.RE
.PP

.SS "\fBlibusb_device_handle\fP * libusb_open_device_with_vid_pid (\fBlibusb_context\fP * ctx, uint16_t vendor_id, uint16_t product_id)"

.PP
Convenience function for finding a device with a particular \fCidVendor\fP/\fCidProduct\fP combination\&. This function is intended for those scenarios where you are using libusb to knock up a quick test application - it allows you to avoid calling \fBlibusb_get_device_list()\fP and worrying about traversing/freeing the list\&.
.PP
This function has limitations and is hence not intended for use in real applications: if multiple devices have the same IDs it will only give you the first one, etc\&.
.PP
\fBParameters\fP
.RS 4
\fIctx\fP the context to operate on, or NULL for the default context 
.br
\fIvendor_id\fP the idVendor value to search for 
.br
\fIproduct_id\fP the idProduct value to search for 
.RE
.PP
\fBReturns\fP
.RS 4
a device handle for the first found device, or NULL on error or if the device could not be found\&. 
.RE
.PP

.SS "void libusb_close (\fBlibusb_device_handle\fP * dev_handle)"

.PP
Close a device handle\&. Should be called on all open handles before your application exits\&.
.PP
Internally, this function destroys the reference that was added by \fBlibusb_open()\fP on the given device\&.
.PP
This is a non-blocking function; no requests are sent over the bus\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP the device handle to close 
.RE
.PP

.SS "\fBlibusb_device\fP * libusb_get_device (\fBlibusb_device_handle\fP * dev_handle)"

.PP
Get the underlying device for a device handle\&. This function does not modify the reference count of the returned device, so do not feel compelled to unreference it when you are done\&. 
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.RE
.PP
\fBReturns\fP
.RS 4
the underlying device 
.RE
.PP

.SS "int libusb_get_configuration (\fBlibusb_device_handle\fP * dev_handle, int * config)"

.PP
Determine the bConfigurationValue of the currently active configuration\&. You could formulate your own control request to obtain this information, but this function has the advantage that it may be able to retrieve the information from operating system caches (no I/O involved)\&.
.PP
If the OS does not cache this information, then this function will block while a control transfer is submitted to retrieve the information\&.
.PP
This function will return a value of 0 in the \fCconfig\fP output parameter if the device is in unconfigured state\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIconfig\fP output location for the bConfigurationValue of the active configuration (only valid for return code 0) 
.RE
.PP
\fBReturns\fP
.RS 4
0 on success 
.PP
\fBLIBUSB_ERROR_NO_DEVICE\fP if the device has been disconnected 
.PP
another LIBUSB_ERROR code on other failure 
.RE
.PP

.SS "int libusb_set_configuration (\fBlibusb_device_handle\fP * dev_handle, int configuration)"

.PP
Set the active configuration for a device\&. The operating system may or may not have already set an active configuration on the device\&. It is up to your application to ensure the correct configuration is selected before you attempt to claim interfaces and perform other operations\&.
.PP
If you call this function on a device already configured with the selected configuration, then this function will act as a lightweight device reset: it will issue a SET_CONFIGURATION request using the current configuration, causing most USB-related device state to be reset (altsetting reset to zero, endpoint halts cleared, toggles reset)\&.
.PP
Not all backends support setting the configuration from user space, which will be indicated by the return code \fBLIBUSB_ERROR_NOT_SUPPORTED\fP\&. As this suggests that the platform is handling the device configuration itself, this error should generally be safe to ignore\&.
.PP
You cannot change/reset configuration if your application has claimed interfaces\&. It is advised to set the desired configuration before claiming interfaces\&.
.PP
Alternatively you can call \fBlibusb_release_interface()\fP first\&. Note if you do things this way you must ensure that auto_detach_kernel_driver for \fCdev\fP is 0, otherwise the kernel driver will be re-attached when you release the interface(s)\&.
.PP
You cannot change/reset configuration if other applications or drivers have claimed interfaces\&.
.PP
A configuration value of -1 will put the device in unconfigured state\&. The USB specifications state that a configuration value of 0 does this, however buggy devices exist which actually have a configuration 0\&.
.PP
You should always use this function rather than formulating your own SET_CONFIGURATION control request\&. This is because the underlying operating system needs to know when such changes happen\&.
.PP
This is a blocking function\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIconfiguration\fP the bConfigurationValue of the configuration you wish to activate, or -1 if you wish to put the device in an unconfigured state 
.RE
.PP
\fBReturns\fP
.RS 4
0 on success 
.PP
\fBLIBUSB_ERROR_NOT_FOUND\fP if the requested configuration does not exist 
.PP
\fBLIBUSB_ERROR_BUSY\fP if interfaces are currently claimed 
.PP
\fBLIBUSB_ERROR_NOT_SUPPORTED\fP if setting or changing the configuration is not supported by the backend 
.PP
\fBLIBUSB_ERROR_NO_DEVICE\fP if the device has been disconnected 
.PP
another LIBUSB_ERROR code on other failure 
.RE
.PP
\fBSee also\fP
.RS 4
\fBlibusb_set_auto_detach_kernel_driver()\fP 
.RE
.PP

.SS "int libusb_claim_interface (\fBlibusb_device_handle\fP * dev_handle, int interface_number)"

.PP
Claim an interface on a given device handle\&. You must claim the interface you wish to use before you can perform I/O on any of its endpoints\&.
.PP
It is legal to attempt to claim an already-claimed interface, in which case libusb just returns 0 without doing anything\&.
.PP
If auto_detach_kernel_driver is set to 1 for \fCdev\fP, the kernel driver will be detached if necessary, on failure the detach error is returned\&.
.PP
Claiming of interfaces is a purely logical operation; it does not cause any requests to be sent over the bus\&. Interface claiming is used to instruct the underlying operating system that your application wishes to take ownership of the interface\&.
.PP
This is a non-blocking function\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIinterface_number\fP the \fCbInterfaceNumber\fP of the interface you wish to claim 
.RE
.PP
\fBReturns\fP
.RS 4
0 on success 
.PP
\fBLIBUSB_ERROR_NOT_FOUND\fP if the requested interface does not exist 
.PP
\fBLIBUSB_ERROR_BUSY\fP if another program or driver has claimed the interface 
.PP
\fBLIBUSB_ERROR_NO_DEVICE\fP if the device has been disconnected 
.PP
a LIBUSB_ERROR code on other failure 
.RE
.PP
\fBSee also\fP
.RS 4
\fBlibusb_set_auto_detach_kernel_driver()\fP 
.RE
.PP

.SS "int libusb_release_interface (\fBlibusb_device_handle\fP * dev_handle, int interface_number)"

.PP
Release an interface previously claimed with \fBlibusb_claim_interface()\fP\&. If the device is still connected, you should release all claimed interfaces before closing a device handle\&. This step is not required, and not useful, for a device that has been disconnected (for example, when handling a \fBLIBUSB_HOTPLUG_EVENT_DEVICE_LEFT\fP event): the call will fail with \fBLIBUSB_ERROR_NO_DEVICE\fP and the kernel/backend will have already released the interfaces associated with the disconnected device\&.
.PP
This is a blocking function\&. A SET_INTERFACE control request will be sent to the device, resetting interface state to the first alternate setting\&.
.PP
If auto_detach_kernel_driver is set to 1 for \fCdev\fP, the kernel driver will be re-attached after releasing the interface\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIinterface_number\fP the \fCbInterfaceNumber\fP of the previously-claimed interface 
.RE
.PP
\fBReturns\fP
.RS 4
0 on success 
.PP
\fBLIBUSB_ERROR_NOT_FOUND\fP if the interface was not claimed 
.PP
\fBLIBUSB_ERROR_NO_DEVICE\fP if the device has been disconnected 
.PP
another LIBUSB_ERROR code on other failure 
.RE
.PP
\fBSee also\fP
.RS 4
\fBlibusb_set_auto_detach_kernel_driver()\fP 
.RE
.PP

.SS "int libusb_set_interface_alt_setting (\fBlibusb_device_handle\fP * dev_handle, int interface_number, int alternate_setting)"

.PP
Activate an alternate setting for an interface\&. The interface must have been previously claimed with \fBlibusb_claim_interface()\fP\&.
.PP
You should always use this function rather than formulating your own SET_INTERFACE control request\&. This is because the underlying operating system needs to know when such changes happen\&.
.PP
This is a blocking function\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIinterface_number\fP the \fCbInterfaceNumber\fP of the previously-claimed interface 
.br
\fIalternate_setting\fP the \fCbAlternateSetting\fP of the alternate setting to activate 
.RE
.PP
\fBReturns\fP
.RS 4
0 on success 
.PP
\fBLIBUSB_ERROR_NOT_FOUND\fP if the interface was not claimed, or the requested alternate setting does not exist 
.PP
\fBLIBUSB_ERROR_NO_DEVICE\fP if the device has been disconnected 
.PP
another LIBUSB_ERROR code on other failure 
.RE
.PP

.SS "int libusb_clear_halt (\fBlibusb_device_handle\fP * dev_handle, unsigned char endpoint)"

.PP
Clear the halt/stall condition for an endpoint\&. Endpoints with halt status are unable to receive or transmit data until the halt condition is stalled\&.
.PP
You should cancel all pending transfers before attempting to clear the halt condition\&.
.PP
This is a blocking function\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIendpoint\fP the endpoint to clear halt status 
.RE
.PP
\fBReturns\fP
.RS 4
0 on success 
.PP
\fBLIBUSB_ERROR_NOT_FOUND\fP if the endpoint does not exist 
.PP
\fBLIBUSB_ERROR_NO_DEVICE\fP if the device has been disconnected 
.PP
another LIBUSB_ERROR code on other failure 
.RE
.PP

.SS "int libusb_reset_device (\fBlibusb_device_handle\fP * dev_handle)"

.PP
Perform a USB port reset to reinitialize a device\&. The system will attempt to restore the previous configuration and alternate settings after the reset has completed\&.
.PP
If the reset fails, the descriptors change, or the previous state cannot be restored, the device will appear to be disconnected and reconnected\&. This means that the device handle is no longer valid (you should close it) and rediscover the device\&. A return code of \fBLIBUSB_ERROR_NOT_FOUND\fP indicates when this is the case\&.
.PP
This is a blocking function which usually incurs a noticeable delay\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a handle of the device to reset 
.RE
.PP
\fBReturns\fP
.RS 4
0 on success 
.PP
\fBLIBUSB_ERROR_NOT_FOUND\fP if re-enumeration is required, or if the device has been disconnected 
.PP
another LIBUSB_ERROR code on other failure 
.RE
.PP

.SS "int libusb_kernel_driver_active (\fBlibusb_device_handle\fP * dev_handle, int interface_number)"

.PP
Determine if a kernel driver is active on an interface\&. If a kernel driver is active, you cannot claim the interface, and libusb will be unable to perform I/O\&.
.PP
This functionality is not available on Windows\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIinterface_number\fP the interface to check 
.RE
.PP
\fBReturns\fP
.RS 4
0 if no kernel driver is active 
.PP
1 if a kernel driver is active 
.PP
\fBLIBUSB_ERROR_NO_DEVICE\fP if the device has been disconnected 
.PP
\fBLIBUSB_ERROR_NOT_SUPPORTED\fP on platforms where the functionality is not available 
.PP
another LIBUSB_ERROR code on other failure 
.RE
.PP
\fBSee also\fP
.RS 4
\fBlibusb_detach_kernel_driver()\fP 
.RE
.PP

.SS "int libusb_detach_kernel_driver (\fBlibusb_device_handle\fP * dev_handle, int interface_number)"

.PP
Detach a kernel driver from an interface\&. If successful, you will then be able to claim the interface and perform I/O\&.
.PP
This functionality is not available on Windows\&.
.PP
Note that libusb itself also talks to the device through a special kernel driver, if this driver is already attached to the device, this call will not detach it and return \fBLIBUSB_ERROR_NOT_FOUND\fP\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIinterface_number\fP the interface to detach the driver from 
.RE
.PP
\fBReturns\fP
.RS 4
0 on success 
.PP
\fBLIBUSB_ERROR_NOT_FOUND\fP if no kernel driver was active 
.PP
\fBLIBUSB_ERROR_INVALID_PARAM\fP if the interface does not exist 
.PP
\fBLIBUSB_ERROR_NO_DEVICE\fP if the device has been disconnected 
.PP
\fBLIBUSB_ERROR_NOT_SUPPORTED\fP on platforms where the functionality is not available 
.PP
another LIBUSB_ERROR code on other failure 
.RE
.PP
\fBSee also\fP
.RS 4
\fBlibusb_kernel_driver_active()\fP 
.RE
.PP

.SS "int libusb_attach_kernel_driver (\fBlibusb_device_handle\fP * dev_handle, int interface_number)"

.PP
Re-attach an interface's kernel driver, which was previously detached using \fBlibusb_detach_kernel_driver()\fP\&. This functionality is not available on Windows\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIinterface_number\fP the interface to attach the driver from 
.RE
.PP
\fBReturns\fP
.RS 4
0 on success 
.PP
\fBLIBUSB_ERROR_NOT_FOUND\fP if no kernel driver was active 
.PP
\fBLIBUSB_ERROR_INVALID_PARAM\fP if the interface does not exist 
.PP
\fBLIBUSB_ERROR_NO_DEVICE\fP if the device has been disconnected 
.PP
\fBLIBUSB_ERROR_NOT_SUPPORTED\fP on platforms where the functionality is not available 
.PP
\fBLIBUSB_ERROR_BUSY\fP if the driver cannot be attached because the interface is claimed by a program or driver 
.PP
another LIBUSB_ERROR code on other failure 
.RE
.PP
\fBSee also\fP
.RS 4
\fBlibusb_kernel_driver_active()\fP 
.RE
.PP

.SS "int libusb_set_auto_detach_kernel_driver (\fBlibusb_device_handle\fP * dev_handle, int enable)"

.PP
Enable/disable libusb's automatic kernel driver detachment\&. When this is enabled libusb will automatically detach the kernel driver on an interface when claiming the interface, and attach it when releasing the interface\&.
.PP
Automatic kernel driver detachment is disabled on newly opened device handles by default\&.
.PP
On platforms which do not have LIBUSB_CAP_SUPPORTS_DETACH_KERNEL_DRIVER this function will return \fBLIBUSB_ERROR_NOT_SUPPORTED\fP, and libusb will continue as if this function was never called\&.
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIenable\fP whether to enable or disable auto kernel driver detachment
.RE
.PP
\fBReturns\fP
.RS 4
\fBLIBUSB_SUCCESS\fP on success 
.PP
\fBLIBUSB_ERROR_NOT_SUPPORTED\fP on platforms where the functionality is not available 
.RE
.PP
\fBSee also\fP
.RS 4
\fBlibusb_claim_interface()\fP 
.PP
\fBlibusb_release_interface()\fP 
.PP
\fBlibusb_set_configuration()\fP 
.RE
.PP

.SS "int libusb_endpoint_supports_raw_io (\fBlibusb_device_handle\fP * dev_handle, uint8_t endpoint)"

.PP
Check if the endpoint supports RAW_IO\&. 
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIendpoint\fP the endpoint to check
.RE
.PP
\fBReturns\fP
.RS 4
1 if the endpoint supports RAW_IO 
.PP
0 if the endpoint does not support RAW_IO 
.PP
a LIBUSB_ERROR code on failure
.RE
.PP
Only endpoints using the WinUSB driver support RAW_IO, for all other backends/drivers this function will return 0\&.
.PP
Fails if the interface the endpoint belongs to isn't claimed, 
.PP
\fBSee also\fP
.RS 4
\fBlibusb_claim_interface()\fP\&.
.PP
\fBlibusb_endpoint_set_raw_io()\fP
.RE
.PP
Since version 1\&.0\&.30, \fBLIBUSB_API_VERSION\fP >= 0x0100010C 
.SS "int libusb_endpoint_set_raw_io (\fBlibusb_device_handle\fP * dev_handle, uint8_t endpoint, int enable)"

.PP
Enable/disable RAW_IO for an endpoint on an open device\&. Only endpoints using the WinUSB driver support RAW_IO, for all other backends/drivers this function will return an error code\&.
.PP
Using RAW_IO can greatly improve USB throughput by directly passing transfer requests to the underlying USB driver instead of queuing them in WinUSB\&. This can be particularly useful for high-throughput devices like cameras or oscilloscopes\&.
.PP
Transfers submitted to the endpoint while RAW_IO is enabled will fail unless they adhere to the following rules :
.IP "\(bu" 2
The buffer length must be a multiple of the maximum endpoint packet size 
.PP
\fBSee also\fP
.RS 4
\fBlibusb_get_max_packet_size\fP\&.
.RE
.PP

.IP "\(bu" 2
The buffer length must be less than or equal to the value returned by \fBlibusb_get_max_raw_io_transfer_size\fP\&.
.PP
.PP
This option should not be changed when any transfer is in progress on the specified endpoint\&.
.PP
Fails if the interface the endpoint belongs to isn't claimed, 
.PP
\fBSee also\fP
.RS 4
\fBlibusb_claim_interface()\fP\&.
.RE
.PP
\fBParameters\fP
.RS 4
\fIdev_handle\fP a device handle 
.br
\fIendpoint\fP the endpoint to set RAW_IO for 
.br
\fIenable\fP 1 to enable RAW_IO, 0 to disable it
.RE
.PP
\fBReturns\fP
.RS 4
\fBLIBUSB_SUCCESS\fP on success 
.PP
\fBLIBUSB_ERROR_NOT_SUPPORTED\fP if the backend does not support RAW_IO\&. 
.PP
another LIBUSB_ERROR code on other failure
.RE
.PP
\fBSee also\fP
.RS 4
\fBlibusb_endpoint_supports_raw_io()\fP 
.PP
https://learn.microsoft.com/en-us/windows-hardware/drivers/usbcon/winusb-functions-for-pipe-policy-modification
.RE
.PP
Since version 1\&.0\&.30, \fBLIBUSB_API_VERSION\fP >= 0x0100010C 
.SS "int libusb_get_max_raw_io_transfer_size (\fBlibusb_device_handle\fP * dev_handle, uint8_t endpoint)"

.PP
Retrieve the maximum transfer size in bytes supported for WinUSB RAW_IO for an inbound bulk or interrupt endpoint on an open device\&. Returns a maximum transfer size in bytes, or a negative error code\&. If the backend does not support WinUSB RAW_IO, returns \fBLIBUSB_ERROR_NOT_SUPPORTED\fP\&.
.PP
Fails if the interface the endpoint belongs to isn't claimed, 
.PP
\fBSee also\fP
.RS 4
\fBlibusb_claim_interface()\fP\&.
.PP
\fBlibusb_endpoint_set_raw_io()\fP
.RE
.PP
Since version 1\&.0\&.30, \fBLIBUSB_API_VERSION\fP >= 0x0100010C 
.SH "Author"
.PP 
Generated automatically by Doxygen for libusb from the source code\&.
