.TH "libusb_hotplug" 3 "Version 1.0.30" "libusb" \" -*- nroff -*-
.ad l
.nh
.SH NAME
libusb_hotplug \- Device hotplug event notification
.PP
 \- This page details how to use the libusb hotplug interface, where available\&.  

.SH SYNOPSIS
.br
.PP
.SS "Macros"

.in +1c
.ti -1c
.RI "#define \fBLIBUSB_HOTPLUG_NO_FLAGS\fP   0"
.br
.RI "Convenience macro when not using any flags\&. "
.ti -1c
.RI "#define \fBLIBUSB_HOTPLUG_MATCH_ANY\fP   \-1"
.br
.RI "Wildcard matching for hotplug events\&. "
.in -1c
.SS "Typedefs"

.in +1c
.ti -1c
.RI "typedef int \fBlibusb_hotplug_callback_handle\fP"
.br
.RI "Callback handle\&. "
.ti -1c
.RI "typedef int(* \fBlibusb_hotplug_callback_fn\fP) (\fBlibusb_context\fP *ctx, \fBlibusb_device\fP *device, \fBlibusb_hotplug_event\fP event, void *user_data)"
.br
.RI "Hotplug callback function type\&. "
.in -1c
.SS "Enumerations"

.in +1c
.ti -1c
.RI "enum \fBlibusb_hotplug_event\fP { \fBLIBUSB_HOTPLUG_EVENT_DEVICE_ARRIVED\fP = (1 << 0), \fBLIBUSB_HOTPLUG_EVENT_DEVICE_LEFT\fP = (1 << 1) }"
.br
.RI "Since version 1\&.0\&.16, \fBLIBUSB_API_VERSION\fP >= 0x01000102\&. "
.ti -1c
.RI "enum \fBlibusb_hotplug_flag\fP { \fBLIBUSB_HOTPLUG_ENUMERATE\fP = (1 << 0) }"
.br
.RI "Since version 1\&.0\&.16, \fBLIBUSB_API_VERSION\fP >= 0x01000102\&. "
.in -1c
.SS "Functions"

.in +1c
.ti -1c
.RI "int \fBlibusb_hotplug_register_callback\fP (\fBlibusb_context\fP *ctx, int events, int flags, int vendor_id, int product_id, int dev_class, \fBlibusb_hotplug_callback_fn\fP cb_fn, void *user_data, \fBlibusb_hotplug_callback_handle\fP *callback_handle)"
.br
.RI "Register a hotplug callback function\&. "
.ti -1c
.RI "void \fBlibusb_hotplug_deregister_callback\fP (\fBlibusb_context\fP *ctx, \fBlibusb_hotplug_callback_handle\fP callback_handle)"
.br
.RI "Deregisters a hotplug callback\&. "
.ti -1c
.RI "void * \fBlibusb_hotplug_get_user_data\fP (\fBlibusb_context\fP *ctx, \fBlibusb_hotplug_callback_handle\fP callback_handle)"
.br
.RI "Gets the user_data associated with a hotplug callback\&. "
.in -1c
.SH "Detailed Description"
.PP 
This page details how to use the libusb hotplug interface, where available\&. 

Be mindful that not all platforms currently implement hotplug notification and that you should first call on \fBlibusb_has_capability()\fP with parameter \fBLIBUSB_CAP_HAS_HOTPLUG\fP to confirm that hotplug support is available\&. 
.SH "Typedef Documentation"
.PP 
.SS "typedef int \fBlibusb_hotplug_callback_handle\fP"

.PP
Callback handle\&. Callbacks handles are generated by \fBlibusb_hotplug_register_callback()\fP and can be used to deregister callbacks\&. Callback handles are unique per libusb_context and it is safe to call \fBlibusb_hotplug_deregister_callback()\fP on an already deregistered callback\&.
.PP
Since version 1\&.0\&.16, \fBLIBUSB_API_VERSION\fP >= 0x01000102
.PP
For more information, see \fBDevice hotplug event notification\fP\&. 
.SS "typedef int(* libusb_hotplug_callback_fn) (\fBlibusb_context\fP *ctx, \fBlibusb_device\fP *device, \fBlibusb_hotplug_event\fP event, void *user_data)"

.PP
Hotplug callback function type\&. When requesting hotplug event notifications, you pass a pointer to a callback function of this type\&.
.PP
This callback may be called by an internal event thread and as such it is recommended the callback do minimal processing before returning\&.
.PP
libusb will call this function later, when a matching event had happened on a matching device\&. See \fBDevice hotplug event notification\fP for more information\&.
.PP
It is safe to call either \fBlibusb_hotplug_register_callback()\fP or \fBlibusb_hotplug_deregister_callback()\fP from within a callback function\&.
.PP
Since version 1\&.0\&.16, \fBLIBUSB_API_VERSION\fP >= 0x01000102
.PP
\fBParameters\fP
.RS 4
\fIctx\fP context of this notification 
.br
\fIdevice\fP libusb_device this event occurred on 
.br
\fIevent\fP event that occurred 
.br
\fIuser_data\fP user data provided when this callback was registered 
.RE
.PP
\fBReturns\fP
.RS 4
bool whether this callback is finished processing events\&. returning 1 will cause this callback to be deregistered 
.RE
.PP

.SH "Enumeration Type Documentation"
.PP 
.SS "enum \fBlibusb_hotplug_event\fP"

.PP
Since version 1\&.0\&.16, \fBLIBUSB_API_VERSION\fP >= 0x01000102\&. Hotplug events 
.PP
\fBEnumerator\fP
.in +1c
.TP
\fB\fILIBUSB_HOTPLUG_EVENT_DEVICE_ARRIVED \fP\fP
A device has been plugged in and is ready to use\&. 
.TP
\fB\fILIBUSB_HOTPLUG_EVENT_DEVICE_LEFT \fP\fP
A device has left and is no longer available\&. It is the user's responsibility to call libusb_close on any handle associated with a disconnected device\&. See \fBDevice hotplug event notification\fP for the set of libusb functions that are safe to call from within a LIBUSB_HOTPLUG_EVENT_DEVICE_LEFT callback\&. 
.SS "enum \fBlibusb_hotplug_flag\fP"

.PP
Since version 1\&.0\&.16, \fBLIBUSB_API_VERSION\fP >= 0x01000102\&. Hotplug flags 
.PP
\fBEnumerator\fP
.in +1c
.TP
\fB\fILIBUSB_HOTPLUG_ENUMERATE \fP\fP
Arm the callback and fire it for all matching currently attached devices\&. 
.SH "Function Documentation"
.PP 
.SS "int libusb_hotplug_register_callback (\fBlibusb_context\fP * ctx, int events, int flags, int vendor_id, int product_id, int dev_class, \fBlibusb_hotplug_callback_fn\fP cb_fn, void * user_data, \fBlibusb_hotplug_callback_handle\fP * callback_handle)"

.PP
Register a hotplug callback function\&. Register a callback with the libusb_context\&. The callback will fire when a matching event occurs on a matching device\&. The callback is armed until either it is deregistered with \fBlibusb_hotplug_deregister_callback()\fP or the supplied callback returns 1 to indicate it is finished processing events\&.
.PP
If the \fBLIBUSB_HOTPLUG_ENUMERATE\fP is passed the callback will be called with a \fBLIBUSB_HOTPLUG_EVENT_DEVICE_ARRIVED\fP for all devices already plugged into the machine\&. Note that libusb modifies its internal device list from a separate thread, while calling hotplug callbacks from \fBlibusb_handle_events()\fP, so it is possible for a device to already be present on, or removed from, its internal device list, while the hotplug callbacks still need to be dispatched\&. This means that when using \fBLIBUSB_HOTPLUG_ENUMERATE\fP, your callback may be called twice for the arrival of the same device, once from \fBlibusb_hotplug_register_callback()\fP and once from \fBlibusb_handle_events()\fP; and/or your callback may be called for the removal of a device for which an arrived call was never made\&.
.PP
Since version 1\&.0\&.16, \fBLIBUSB_API_VERSION\fP >= 0x01000102
.PP
\fBParameters\fP
.RS 4
\fIctx\fP context to register this callback with 
.br
\fIevents\fP bitwise or of hotplug events that will trigger this callback\&. See \fBlibusb_hotplug_event\fP 
.br
\fIflags\fP bitwise or of hotplug flags that affect registration\&. See \fBlibusb_hotplug_flag\fP 
.br
\fIvendor_id\fP the vendor id to match or \fBLIBUSB_HOTPLUG_MATCH_ANY\fP 
.br
\fIproduct_id\fP the product id to match or \fBLIBUSB_HOTPLUG_MATCH_ANY\fP 
.br
\fIdev_class\fP the device class to match or \fBLIBUSB_HOTPLUG_MATCH_ANY\fP 
.br
\fIcb_fn\fP the function to be invoked on a matching event/device 
.br
\fIuser_data\fP user data to pass to the callback function 
.br
\fIcallback_handle\fP pointer to store the handle of the allocated callback (can be NULL) 
.RE
.PP
\fBReturns\fP
.RS 4
\fBLIBUSB_SUCCESS\fP on success LIBUSB_ERROR code on failure 
.RE
.PP

.SS "void libusb_hotplug_deregister_callback (\fBlibusb_context\fP * ctx, \fBlibusb_hotplug_callback_handle\fP callback_handle)"

.PP
Deregisters a hotplug callback\&. Deregister a callback from a libusb_context\&. This function is safe to call from within a hotplug callback\&.
.PP
Since version 1\&.0\&.16, \fBLIBUSB_API_VERSION\fP >= 0x01000102
.PP
\fBParameters\fP
.RS 4
\fIctx\fP context this callback is registered with 
.br
\fIcallback_handle\fP the handle of the callback to deregister 
.RE
.PP

.SS "void * libusb_hotplug_get_user_data (\fBlibusb_context\fP * ctx, \fBlibusb_hotplug_callback_handle\fP callback_handle)"

.PP
Gets the user_data associated with a hotplug callback\&. Since version v1\&.0\&.24 \fBLIBUSB_API_VERSION\fP >= 0x01000108
.PP
\fBParameters\fP
.RS 4
\fIctx\fP context this callback is registered with 
.br
\fIcallback_handle\fP the handle of the callback to get the user_data of 
.RE
.PP

.SH "Author"
.PP 
Generated automatically by Doxygen for libusb from the source code\&.
